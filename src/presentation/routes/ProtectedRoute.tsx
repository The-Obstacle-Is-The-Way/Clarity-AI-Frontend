// Placeholder for Protected Route Component
import React from 'react';

const ProtectedRoute: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  // Add auth check logic here later
  return <>{children}</>;
};

export default ProtectedRoute;
